## Goal
Bootstrap project — docs structure, branching strategy, Vue scaffold.

## Completed
GEN-007-git-branching-strategy.md — PR workflow documented and branching conventions set.
GEN-001-docker-scaffold.md — Vue 3 + Vite + Tailwind v3 + Inter scaffold generated via Docker.

Note: GitHub repo was accidentally deleted mid-session when exploring branch protection (org move gone wrong). Repo recreated at same URL, all branches restored via local push.

## Left off at
development clean and up to date. Dev compose not yet written. App not yet running.

## Next step
GEN-002-docker-dev-prod.md — write docker-compose.yml (dev, hot-reload, port 5173), confirm app runs.
